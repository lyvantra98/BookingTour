<% provide :title, t("devises.change_your_password") %>
<div class="col-lg-12 col-md-12 col-sx-12">
  <h2 class="text-center"><%= t "devises.change_your_password" %></h2>
</div>
<%= form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :put } do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>
  <div class="col-lg-8 col-md-8 col-lg-offset-2">
    <%= f.label :password, t("devises.new_password") %><br />
    <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> <%= t "devises.characters_minimum" %>)</em><br />
    <% end %>
    <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control"%><br/>
    <%= f.label :password_confirmation, t("devises.confirm_new_password") %><br/>
    <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %><br/>
  </div>
  <div class="col-lg-8 col-md-8 col-lg-offset-2">
    <%= f.submit t("devises.change_your_password"), class: "btn btn-primary" %><br/>
  </div>
<% end %>
 <div class="col-lg-8 col-md-8 col-lg-offset-2">
  <%= render "devise/shared/links" %>
</div>
